# BME Image Segmentation

This repository is planned to host two medical image segmentation code paths:

- `seg2d/`: segmentation workflows for 2D image data.
- `seg3d/`: segmentation workflows for 3D volumetric data.

Implementation details will be added after the data format, model choices, training
pipeline, and evaluation protocol are aligned.
